To switch between 32-bit and 64-bit executables:
change CC_X86, CXX_X86 variables in Makefile.build

To build:
make specbuild2006  # this may take some time. copy spec_cpu_2000.tar.bz2 in ../tars beforehand

To run
make specrun2006
